# LEGADO - nao e mais usado. Console sem basic auth desde fix api/v1/session 401.
# Mantido apenas como referencia se quiser reativar basic auth isolado por location.
#   cp security/.htpasswd.example security/.htpasswd
#   openssl passwd -apr1
# Then replace REPLACE_WITH_GENERATED_HASH with the output.
admin:REPLACE_WITH_GENERATED_HASH
